{
  "name": "daily-quiz",
  "version": "0.1.0",
  "private": true,
  "description": "Ferguson Training daily quiz: trainer hosts a timed quiz, interns join from laptop or phone over the room Wi-Fi.",
  "type": "module",
  "engines": { "node": ">=22.5" },
  "scripts": {
    "start": "node server/index.js",
    "test": "node --test \"tests/**/*.test.js\"",
    "validate:questions": "node scripts/validate-questions.mjs"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
