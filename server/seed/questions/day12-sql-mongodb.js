// Session: Data Stores (Day 12) — subtopics: Azure SQL / T-SQL basics, joins, stored procedures; MongoDB NoSQL concepts, collections, CRUD, basic aggregation
export default [
  {
    text: "Which T-SQL clause filters individual rows before any grouping happens?",
    options: ["WHERE", "HAVING", "ORDER BY", "GROUP BY"],
    answer: 0,
    complexity: "easy",
    explanation: "WHERE is applied to rows before aggregation; HAVING filters the groups produced afterwards.",
  },
  {
    text: "Which T-SQL statement adds a new row to a table?",
    options: ["UPDATE", "INSERT", "ALTER", "CREATE"],
    answer: 1,
    complexity: "easy",
    explanation: "INSERT adds rows, UPDATE changes existing ones, and ALTER changes the table definition rather than the data.",
  },
  {
    text: "In MongoDB, which container is the closest counterpart to a relational table?",
    options: ["A database", "A document", "A collection", "An index"],
    answer: 2,
    complexity: "easy",
    explanation: "A collection holds documents, which correspond to rows; a database holds collections.",
  },
  {
    text: "Which MongoDB method inserts exactly one document into a collection?",
    options: ["db.orders.addOne()", "db.orders.push()", "db.orders.putOne()", "db.orders.insertOne()"],
    answer: 3,
    complexity: "easy",
    explanation: "insertOne() writes a single document and returns its _id; insertMany() takes an array.",
  },
  {
    text: "Which join returns only the rows that have a match in both tables?",
    options: ["LEFT JOIN", "INNER JOIN", "FULL OUTER JOIN", "CROSS JOIN"],
    answer: 1,
    complexity: "easy",
    explanation: "INNER JOIN keeps matched pairs only. Outer joins keep unmatched rows and pad the other side with NULLs.",
  },
  {
    text: "In which binary format does MongoDB store documents?",
    options: ["CSV", "XML", "BSON", "YAML"],
    answer: 2,
    complexity: "easy",
    explanation: "BSON is a binary encoding of JSON-like documents that adds types such as ObjectId, Date and Decimal128.",
  },
  {
    text: "Which T-SQL keyword removes duplicate rows from a result set?",
    options: ["UNIQUE", "SEPARATE", "FILTER", "DISTINCT"],
    answer: 3,
    complexity: "easy",
    explanation: "SELECT DISTINCT collapses rows that are identical across every selected column.",
  },
  {
    text: "Which MongoDB call returns every document whose status field equals \"active\"?",
    options: [
      "db.orders.find({ status: \"active\" })",
      "db.orders.find(status = \"active\")",
      "db.orders.select({ status: \"active\" })",
      "db.orders.where(\"status\", \"active\")",
    ],
    answer: 0,
    complexity: "easy",
    explanation: "find() takes a query document whose fields are matched for equality unless an operator such as $gt is used.",
  },
  {
    text: "A LEFT JOIN finds no matching row on the right side. What appears in the right table's columns?",
    options: ["Zeros", "Empty strings", "NULLs", "The row is dropped"],
    answer: 2,
    complexity: "medium",
    explanation: "The left row is kept and every right-hand column is padded with NULL, which is how you find rows without matches.",
  },
  {
    text: "Which clause filters on an aggregate such as COUNT(*) > 5?",
    options: ["WHERE", "ON", "TOP", "HAVING"],
    answer: 3,
    complexity: "medium",
    explanation: "HAVING runs after GROUP BY, so it can test aggregate values; WHERE runs before the aggregates exist.",
  },
  {
    text: "What is a practical benefit of a stored procedure over the same SQL built in application code?",
    options: [
      "One place to change logic, plus plan reuse",
      "It removes the need for indexes",
      "It guarantees that deadlocks cannot occur",
      "It replaces the need for transactions",
    ],
    answer: 0,
    complexity: "medium",
    explanation: "The logic lives with the data, callers pass parameters instead of concatenating SQL, and the cached plan is reused.",
  },
  {
    text: "How is an input parameter declared for a T-SQL stored procedure?",
    options: [
      "CREATE PROC p WITH @id INT AS",
      "CREATE PROC p @id INT AS",
      "CREATE PROC p (INT id) AS",
      "CREATE PROC p PARAM id INT AS",
    ],
    answer: 1,
    complexity: "medium",
    explanation: "Parameters are listed after the procedure name as @name type, optionally with a default value, before the AS keyword.",
  },
  {
    text: "Which MongoDB call modifies only the first document matching the filter?",
    options: ["updateMany()", "replaceMany()", "modifyOne()", "updateOne()"],
    answer: 3,
    complexity: "medium",
    explanation: "updateOne() stops after the first match; updateMany() applies the same update to every matching document.",
  },
  {
    text: "In a MongoDB update, what does the $set operator do?",
    options: [
      "Sets the named fields and leaves others intact",
      "Replaces the whole document",
      "Creates a new collection",
      "Renames the document's _id",
    ],
    answer: 0,
    complexity: "medium",
    explanation: "$set updates or adds only the listed fields. Passing a bare document without operators replaces the whole thing.",
  },
  {
    text: "Which aggregation stage groups documents together and computes totals such as a sum?",
    options: ["$match", "$group", "$project", "$sort"],
    answer: 1,
    complexity: "medium",
    explanation: "$group buckets documents by its _id expression and applies accumulators like $sum, $avg and $max.",
  },
  {
    text: "In an aggregation pipeline, what does the $match stage do?",
    options: [
      "Joins two collections",
      "Reshapes each document",
      "Filters documents, like a SQL WHERE",
      "Sorts the output documents",
    ],
    answer: 2,
    complexity: "medium",
    explanation: "$match takes a normal query document and passes only the matching documents to the next stage.",
  },
  {
    text: "Why should $match usually appear as early as possible in an aggregation pipeline?",
    options: [
      "It cuts the documents flowing into later stages",
      "It must legally be the first stage",
      "It forces a full collection scan",
      "It commits the pipeline's writes",
    ],
    answer: 0,
    complexity: "medium",
    explanation: "Filtering first reduces the work of later stages and lets an early $match use an index on the collection.",
  },
  {
    text: "How does COUNT(col) differ from COUNT(*) in T-SQL?",
    options: [
      "They always return the same number",
      "COUNT(col) ignores rows where col is NULL",
      "COUNT(col) counts distinct values",
      "COUNT(col) sums the values in col",
    ],
    answer: 1,
    complexity: "medium",
    explanation: "COUNT(*) counts rows, while COUNT(col) counts non-NULL values in that column. COUNT(DISTINCT col) counts unique values.",
  },
  {
    text: "A LEFT JOIN is followed by `WHERE b.status = 'open'`, where b is the right table. What is the effect?",
    options: [
      "Unmatched left rows are still returned",
      "The query becomes a FULL OUTER JOIN",
      "It behaves like an INNER JOIN",
      "The WHERE clause is ignored",
    ],
    answer: 2,
    complexity: "hard",
    explanation: "Unmatched rows carry NULL in b.status, which fails the predicate, so they disappear. Move the condition into the ON clause to keep them.",
  },
  {
    text: "A stored procedure runs several INSERTs that must all succeed or all be undone. What should it use?",
    options: [
      "SET NOCOUNT ON",
      "A table variable",
      "WITH RECOMPILE",
      "BEGIN TRAN with TRY...CATCH and ROLLBACK",
    ],
    answer: 3,
    complexity: "hard",
    explanation: "An explicit transaction makes the statements atomic; TRY...CATCH rolls back and rethrows so callers see the failure.",
  },
  {
    text: "Queries filter a large MongoDB collection on customerId, which has no index. What does the server do?",
    options: [
      "Rejects the query as unsupported",
      "Scans every document in the collection",
      "Silently uses the _id index instead",
      "Caches the result set automatically",
    ],
    answer: 1,
    complexity: "hard",
    explanation: "Without a usable index MongoDB performs a collection scan, so latency grows with collection size. explain() reveals the COLLSCAN.",
  },
  {
    text: "In MongoDB, what does an update with upsert true do when no document matches the filter?",
    options: [
      "Returns a duplicate key error",
      "Does nothing and reports zero matches",
      "Inserts a document built from filter and update",
      "Deletes the nearest matching document",
    ],
    answer: 2,
    complexity: "hard",
    explanation: "Upsert inserts a new document combining the equality fields of the filter with the update, which makes sync jobs idempotent.",
  },
  {
    text: "`SELECT * FROM orders WHERE YEAR(order_date) = 2024` is slow despite an index on order_date. Why?",
    options: [
      "YEAR is not a valid T-SQL function",
      "The column must be VARCHAR",
      "The literal 2024 must be quoted",
      "Wrapping the column in a function blocks index seeks",
    ],
    answer: 3,
    complexity: "hard",
    explanation: "The predicate is not sargable. Rewrite it as order_date >= '20240101' AND order_date < '20250101' so the index can be sought.",
  },
  {
    text: "When is embedding related data in a MongoDB document a better fit than referencing another collection?",
    options: [
      "When it is read together and bounded in size",
      "When the embedded array grows without limit",
      "When many other documents must update it",
      "When cross-collection joins are the main access path",
    ],
    answer: 0,
    complexity: "hard",
    explanation: "Embedding suits data always read with its parent and small enough to stay under the document size limit; unbounded growth needs references.",
  },
];
