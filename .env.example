# Copy to .env (never committed). The app needs the Postgres connection string.
# Laptop: the database's EXTERNAL connection string from the Render dashboard.
DATABASE_URL=postgresql://user:password@host.oregon-postgres.render.com/dbname
# Optional: keeps sign-ins valid across restarts (any long random string).
# SESSION_SECRET=
